# Assets Directory

This directory contains application assets including images, icons, and other media files.

## Structure

- `images/` - Application images and graphics

## Adding Assets

To add a new asset:
1. Place the file in the appropriate subdirectory
2. Reference it in `pubspec.yaml` under the `flutter.assets` section
3. Access it in code using `AssetImage` or `Image.asset()`
